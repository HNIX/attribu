<%= form_for @source, :html => { :class => "form-horizontal source" } do |f| %>
<div class="columns">
    <% if @source.errors.any? %>
      <div id="error_expl" class="panel panel-danger">
        <div class="panel-heading">
          <h3 class="panel-title"><%= pluralize(@source.errors.count, "error") %> prohibited this source from being saved:</h3>
        </div>
        <div class="panel-body">
          <ul>
          <% @source.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      </div>
    <% end %>
      <div class="column is-12">
          <div class="control-material is-secondary has-icon">
              <%= f.text_field :name, :class => 'material-input' %>
              <span class="material-highlight"></span>
              <span class="bar"></span>
              <%= f.label :name %>
          </div>
          <div class="control-material is-secondary has-icon">
              <%= f.text_field :medium, :class => 'material-input' %>
              <span class="material-highlight"></span>
              <span class="bar"></span>
              <%= f.label :medium %>
          </div>

          <div class="control-material is-secondary has-icon">
              <%= f.text_field :url, :class => 'material-input' %>
              <span class="material-highlight"></span>
              <span class="bar"></span>
              <%= f.label :url %>
          </div>

          <div class="has-text-centered mt-30">
            <%= f.submit nil, :class => 'button btn-dash secondary-btn is-raised is-fullwidth rounded no-lh' %>
          </div>
      </div>

  </div>
<% end %>
